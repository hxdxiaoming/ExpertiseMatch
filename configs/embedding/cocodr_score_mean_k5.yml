experiment_name: COCO-DR Score Mean K5
encoder_class: COCO_DREncoder
encoder_config:
  model_name: models/coco-dr/base-msmarco
  device: null
  batch_size: 16
  max_length: 512
strategy_class: ScoreAggregationStrategy
strategy_config:
  k: 5
  aggregation: mean
  similarity: cosine
description: COCO-DR模型使用Score聚合策略，平均聚合前25篇论文的分数
