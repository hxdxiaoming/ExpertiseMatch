experiment_name: Qwen3 Score Weighted K25
encoder_class: SentenceTransformerEncoder
encoder_config:
  model_name: models/sentence-transformers/qwen3-0.6B
  device: null
  batch_size: 4
  max_length: 512
  trust_remote_code: true
strategy_class: ScoreAggregationStrategy
strategy_config:
  k: 25
  aggregation: weighted_mean
  similarity: cosine
description: Qwen3模型使用Score聚合策略，对前25篇论文进行加权平均
