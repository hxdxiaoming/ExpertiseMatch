experiment_name: COCO-DR Profile Max K25
encoder_class: COCO_DREncoder
encoder_config:
  model_name: models/coco-dr/base-msmarco
  device: null
  batch_size: 16
  max_length: 512
strategy_class: ProfileAggregationStrategy
strategy_config:
  k: 25
  aggregation: max
  similarity: cosine
description: COCO-DR模型使用Profile聚合策略，最大值聚合前25篇论文
