experiment_name: SPECTER2-BASE Score-level Weighted_Mean Aggregation (k=5)
encoder_class: SPECTER2Encoder
encoder_config:
  model_name: models/specter2/base
  device: null
  batch_size: 8
  max_length: 512
strategy_class: ScoreAggregationStrategy
strategy_config:
  k: 5
  aggregation: weighted_mean
  similarity: cosine
description: 'SPECTER2-BASE Score-level加权平均聚合实验（k=5）：

  - 策略②：先比较，后聚合

  - 计算查询论文与审稿人每篇论文的余弦相似度

  - 选择前25个最高相似度分数

  - 对这些分数进行位置加权平均（排名越高权重越大）

  - 精度最高的匹配策略

  '
