experiment_name: SciNCL Score-level Max Aggregation (k=10)
encoder_class: SentenceTransformerEncoder
encoder_config:
  model_name: models/sentence-transformers/scincl
  device: null
  batch_size: 16
  max_length: 512
strategy_class: ScoreAggregationStrategy
strategy_config:
  k: 10
  aggregation: max
  similarity: cosine
description: 'SciNCL Score-level最大值聚合实验（k=10）：

  - 策略②：先比较，后聚合

  - 计算查询论文与审稿人每篇论文的余弦相似度

  - 选择前25个最高相似度分数

  - 取最大相似度分数作为最终匹配分数

  - 关注最强匹配信号

  '
